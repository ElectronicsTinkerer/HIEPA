# TODO

## Functional Stuff (Features)

* Add support for byte selection operators on 16 bit values
* Add MACRO support
* Add INClude support
* Add remaining opcodes
* Verify addressing mode parser (parseargs()) is still working after rewrite of instruction table
* Add CLI args for passing ROM size, input file
* Add * as current PC syntax
* Move from line numbers in assembler messages to printing the assembly line
* Remove comment removal from Assembeler since it is now in Preprocessor

## Code Improvements

* Add single function "Messages.py" > "msg(level, msg)" to print info/warn/error messages
